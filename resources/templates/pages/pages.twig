{% extends '@' ~ lib_prefix ~ 'themes/base.twig' %}
{% block body %}
    {% if which_page is same as('list') %}
        {% include '@' ~ lib_prefix ~ 'snippets/page_list.twig' %}
    {% else %}
        {% include '@' ~ lib_prefix ~ 'snippets/page_form.twig' %}
    {% endif %}
{% endblock %}
