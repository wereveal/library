{% extends '@' ~ twig_prefix ~ 'default/base.twig' %}
{% block body %}
            <div class="container">
                <ul class="manager-links">
                    {% for link in links %}
                        <li>
                            {% include '@' ~ twig_prefix ~ 'elements/anchor.twig' %} - {{ link.description }}
                        </li>
                    {% endfor %}
                </ul>
            </div>
{% endblock %}
{#
    expected array
        [
            'lang',
            'charset',
            'base_url',
            'description',
            'public_dir',
            'title',
            'links',
            'site_url',
            'rights_holder'
        ]
#}
