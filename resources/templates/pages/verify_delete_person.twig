{% extends '@default/base.twig' %}
{% block stylesheets %}{% include '@snippets/base_css.twig' %}{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-12">
            <h1 class="red">Please Verify</h1>
            <p>Please verify you wish to delete the person '{{ real_name }}' ({{ login_id }}.</p>
            <form action="{{ public_dir }}/manager/people/" method="post">
                <button type="submit" name="submit" value="delete" class="btn btn-primary btn-xs">Delete Person</button>
                <input type="hidden" name="people_id" value="{{ people_id }}" />
                <input type="hidden" name="tolken" value="{{ tolken }}" />
                <input type="hidden" name="form_ts" value="{{ form_ts }}" />
                <label for="hobbit" class="hobbit">Enter Name Here</label><input type="text" id="hobbit" name="hobbit" value="" class="hobbit" size="25" maxlength="25" />
            </form>
        </div>
    </div>
{% endblock %}
