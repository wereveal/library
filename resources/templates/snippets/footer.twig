        {% if a_shared.footer.c_content %}
            {% set contentValues = {
                'ctype': a_shared.footer.c_type,
                'content': a_shared.footer.c_content
            }
            %}
            {{ include(lib_snippets ~ 'by_content_type.twig', contentValues, with_context = false) }}
        {% endif %}
