        {% if a_content.header.c_content %}
        <header class="container container-header">
            {% set contentValues = {
                'ctype': a_shared.header.c_type,
                'content': a_shared.header.c_content
            }
            %}
            {{ include(lib_snippets ~ 'by_content_type.twig', contentValues, with_context = false) }}
        </header>
        {% endif %}
