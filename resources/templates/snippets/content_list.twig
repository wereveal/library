       <div class="row list-header sticky-top">
           <div class="col-12 col-md-4">
               Page
           </div>
           <div class="col-12 col-md-4">
                Block
           </div>
           <div class="col-12 col-md-2 offset-md-2">
                &nbsp;
           </div>
       </div>
{% set row_class = 'odd' %}
       <div class="row {{ row_class }}"><div class="col-md-2 offset-md-10">
            <form action="{{ public_dir }}/manager/config/content/" method="post" class="form">
                <button type="submit" name="submit" value="new_content" class="btn btn-primary">New Content</button>
                {% include '@' ~ lib_prefix ~ 'forms/hidden_form_stuff.twig' %}
            </form>
       </div></div>
{% for row in a_content %}
{% set row_class = row_class == 'odd' ? 'even' : 'odd' %}
        <form action="{{ public_dir }}/manager/config/content/" method="post" class="form">
        <div class="row {{ row_class }}">
            <div class="col-12 col-md-2">
                {{ row.page }}
            </div>
            <div class="col-12 col-md-2">
                {{ row.c_block }}
            </div>
            <div class="col-12 col-md-6">
                {{ row.c_content[0:40] }}
            </div>
            <div class="col-12 col-md-2">
                <button type="submit" name="submit" value="edit_content" class="btn btn-primary">Edit Content</button>
                <input type="hidden" name="content_id" value="{{ row.c_id }}">
                {% include '@' ~ lib_prefix ~ 'forms/hidden_form_stuff.twig' %}
            </div>
        </div>
        </form>
{% endfor %}
