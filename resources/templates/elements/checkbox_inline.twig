<div class="form-check form-check-inline">
    <input type="checkbox" id="{{ id }}" name="{{ name }}" value="{{ value }}" class="form-check-input"{{ checked }}>
    <label for="{{ id }}" class="form-check-label">{{ label }}</label>
</div>
