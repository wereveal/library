{% extends 'WerFrameworkBundle:Main:base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
            <div class="row-fluid">
                <div class="span12">
                    <ul>
                        {% for field in fields %}
                            {{ include('WerGuideBundle:Manager:field-block.html.twig', {'field': field}) }}
                        {% endfor %}
                    </ul>
                </div>
            </div>
{% endblock %}
